version: "3"
interval: 500ms
tasks:
  default:
    cmds:
      - task: env-reload
  env-reload:
    cmds:
      - direnv reload
      - nix-direnv-reload
    aliases:
      - env
  run:
    cmds:
      - nix run .
  debug:
    deps:
      - build-debug
    cmds:
      - gdb debug/main --tui
    aliases:
      - d
  debug-dap:
    cmds:
      - gdb debug/main -i dap
  build:
    cmds:
      - nix build .
    aliases:
      - b
  build-test:
    deps:
      - mkdir-debug
    cmds:
      - eval "$TEST_CMD"
    aliases:
      - test
    env:
      src: ./src/main/
  mkdir-debug:
    cmds:
      - rm -r debug || true
      - mkdir debug
    internal: true
  build-debug:
    deps:
      - mkdir-debug
    cmds:
      - eval "$DEBUG_CMD"
    aliases:
      - bd
    env:
      src: ./src/main/
  watch:
    watch: true
    sources:
      - "src/**/*.odin"
    cmds:
      - task: test
  watch-run:
    sources:
      - "src/**/*.odin"
    cmds:
      - task: run
